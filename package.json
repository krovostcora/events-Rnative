{
  "name": "events-rnative",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "install:all": "npm install",
    "start:admin": "npm --workspace=@events/admin-app run start",
    "start:user": "npm --workspace=@events/user-app run start"
  }
}
